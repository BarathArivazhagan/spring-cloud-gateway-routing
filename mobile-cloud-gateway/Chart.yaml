apiVersion: v2
name: mobile-cloud-gateway
description: A Helm chart for the spring cloud gateway routing example

type: application

# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0

appVersion: "0.1.0"
